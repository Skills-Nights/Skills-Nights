import React from 'react'

export default function Loader() {
    return (
        <div>
            <hr/>
            This is the loader file we'll use if loader animation is required...
            <hr/>
        </div>
    )
}
